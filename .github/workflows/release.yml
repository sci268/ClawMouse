# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: release

on:
  workflow_dispatch:

permissions:
  contents: write

env:
  clawmouse_version_suffix: _v0_1_0
  tag_name: v0.1.0

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@main
      - name: Set up Python 3.10
        uses: actions/setup-python@main
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: |
          pip3 install -r requirements-windows.txt
          pip3 install pyinstaller
      - name: Bundle Packages
        run: |
          pyinstaller -Fw --icon=Mondrian.ico --add-data './assets;assets' ClawMouse.py
      - name: Copy artifact
        run: |
          mkdir artifact && mv dist/ClawMouse.exe ./artifact/ClawMouse${{ env.clawmouse_version_suffix }}-win.exe
      - name: Artifact
        uses: actions/upload-artifact@main
        with:
          name: windows
          path: ${{ github.workspace }}/artifact/
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: ClawMouse ${{ env.tag_name }}
          tag_name: ${{ env.tag_name }}
          files: ./artifact/ClawMouse${{ env.clawmouse_version_suffix }}-win.exe

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - name: Set up Python 3.10
        uses: actions/setup-python@main
        with:
          python-version: "3.10.16"
      - name: Install dependencies
        run: |
          pip3 install -r requirements-universal.txt
          pip3 install pyinstaller
      - name: Bundle Packages
        run: |
          pyinstaller -Fw --add-data './assets:assets' --hidden-import "pynput.keyboard._xorg" --hidden-import "pynput.mouse._xorg" --hidden-import "pynput.keyboard._uinput" --hidden-import "pynput.mouse._uinput" ClawMouse.py
      - name: Copy artifact
        run: |
          mkdir artifact && mv dist/ClawMouse ./artifact/ClawMouse${{ env.clawmouse_version_suffix }}-linux
      - name: Artifact
        uses: actions/upload-artifact@main
        with:
          name: linux
          path: ${{ github.workspace }}/artifact/
#      - name: Create Release
#        uses: softprops/action-gh-release@v2
#        with:
#          draft: true
#          name: KeymouseGo${{ env.keymousego-version }}
#          tag_name: ${{ env.tag_name }}
#          files: ./artifact/KeymouseGo${{ env.keymousego-version }}-linux

  build-linux-arm64:
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@main
      - name: Set up Python 3.10
        uses: actions/setup-python@main
        with:
          python-version: "3.10.16"
      - name: Install dependencies
        run: |
          pip3 install -r requirements-universal.txt
          pip3 install pyinstaller
      - name: Bundle Packages
        run: |
          pyinstaller -Fw --add-data './assets:assets' --hidden-import "pynput.keyboard._xorg" --hidden-import "pynput.mouse._xorg" --hidden-import "pynput.keyboard._uinput" --hidden-import "pynput.mouse._uinput" ClawMouse.py
      - name: Copy artifact
        run: |
          mkdir artifact && mv dist/ClawMouse ./artifact/ClawMouse${{ env.clawmouse_version_suffix }}-linux-aarch64
      - name: Artifact
        uses: actions/upload-artifact@main
        with:
          name: linux-aarch64
          path: ${{ github.workspace }}/artifact/
#      - name: Create Release
#        uses: softprops/action-gh-release@v2
#        with:
#          draft: true
#          name: KeymouseGo${{ env.keymousego-version }}
#          tag_name: ${{ env.tag_name }}
#          files: ./artifact/KeymouseGo${{ env.keymousego-version }}-linux

  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@main
      - name: Set up Python 3.10
        uses: actions/setup-python@main
        with:
          python-version: "3.10.11"
      - name: Install dependencies
        run: |
          pip3 install -r requirements-universal.txt
          pip3 install pyinstaller
      - name: Bundle Packages
        run: |
          pyinstaller -Fw --add-data './assets:assets' --hidden-import "pynput.keyboard._darwin" --hidden-import "pynput.mouse._darwin" ClawMouse.py
      - name: Copy artifact
        run: |
          mkdir artifact && mv dist/ClawMouse.app ./artifact/ClawMouse${{ env.clawmouse_version_suffix }}-mac
      - name: Artifact
        uses: actions/upload-artifact@main
        with:
          name: macos
          path: ${{ github.workspace }}/artifact/
#      - name: Create Release
#        uses: softprops/action-gh-release@v2
#        with:
#          draft: true
#          name: KeymouseGo${{ env.keymousego-version }}
#          tag_name: ${{ env.tag_name }}
#          files: ./artifact/KeymouseGo${{ env.keymousego-version }}-mac
